# Memory Index

## Standing rules / feedback

- [NativeWind v4 HAS a real CSS var layer](nativewind-has-a-real-css-var-layer.md) — "RN ⇒ no CSS vars" is false; check `metro.config.js`. JS palette is a TWIN, not derived → ship a parity gate.
- [Flat tailwind color override kills the scale](tailwind-flat-color-override-kills-scale.md) — `red:'#E4202B'` in `extend.colors` makes every `text-red-400` compile to NOTHING; verify with `resolveConfig`, never a stale `.next/` chunk.
- [Calibrate DESIGN.md length to repo maturity](feedback-calibrate-design-md-length.md) — a padded taste cache harms twice (down-routes AND points at fiction); "this is a scaffold" is an allowed verdict.

## Extraction craft

- [RN with NativeWind ruled OUT needs a different gate](design-md-react-native-gate.md) — Tailwind gate is a permanent uninformative 0; use style-position hex + theme-import adoption, exclude categorical palettes.
- [Trading products: P&L channel + tabular figures](design-md-trading-product-pnl-tokens.md) — direction (buy/sell) vs status (success/destructive) is half-merged in both MTM repos; money defaults to proportional numerals.
- [RN DESIGN.md gates](react-native-design-md-gates.md) — settle NativeWind-or-not on 6 axes (NOT the `global.css` filename); if not, gate raw color literals outside the theme module, not palette classes.
- [Themeable tokens need a reach count](themeable-tokens-need-a-reach-count.md) — a white-label layer can be perfectly wired and reach 0 components; measure consumers per token, add an inverse ratchet.
- [Generated + WebView code breaks the color gate](design-md-react-native-extraction.md) — 🔴 a 566KB SINGLE LINE inside a normal .tsx (length-filter it, `grep -c` undercounts to 1); a WebView JS twin needs a parity gate at baseline 0.
